# Movie Database Explorer (utspeng)

Aplikasi pencarian film sederhana menggunakan **ReactJS** dan **OMDb API**.

## Cara Menjalankan
1. ganti file .env (VITE_OMDB_API_KEY="API OMDb anda") 
2. Install dependencies:
   ```bash
   npm install
   npm run start
